# 列Bの計測 運用手順（GPU機で作業する）

手を動かすための覚書。**なぜそうなっているか**は `README.md` を見る。

このマシン（RTX 5050 Laptop 8GB）でやることは**1つだけ**。
「GPUと世代更新で、道具呼び出しがどれだけ安定するか」を測って数字を持ち帰る。
費用は**0円**（ローカル実行のみ）。APIキーは要らない。

---

## 0. 最初の一回だけ

```
git clone https://github.com/ossudesu-lab/kaigo_mcp.git
cd kaigo_mcp
pip install -r requirements.txt
ollama pull qwen3.5:9b
```

動作確認した版（メイン機側）: Python 3.14.4 / mcp 2.0.0 / openai 3.3.1

### Windows で文字化けするとき

PowerShell の既定が cp932 なので、日本語でエラーになることがある。

```
$env:PYTHONIOENCODING = "utf-8"
```

### まず疎通だけ確認する

```
python scripts/smoke_test.py
```

「すべて通過」と出れば MCP サーバーは動いている。ここが通らないうちは先へ進まない。

---

## 1. 道具呼び出しの安定性を測る（本題）

> **1 も 2 も済んでいる**（下の 1-1 / 1-2 / 2-1 / 2-2 を見る）。
> このマシンでやることは残っていない。次は列C・Dだが、**課金があるので勝手に回さない**
> （「やらないこと」を見る）。再測するときは、まず `git pull` すること。

```
python scripts/probe_tool_calling.py --model qwen3.5:9b --runs 6
python scripts/probe_tool_calling.py --model qwen3.5:9b --runs 6 --temperature 0
```

失敗するときは `<tool_call>` の開始タグが壊れて（`olith` `pering` のような
数文字が先頭に付く）呼び出しが本文テキストに漏れる。閉じタグ `</tool_call>` だけが
残るのが目印。スクリプトが該当行を出すので、そのまま控えておく。

### 1-1. 済んだこと（2026-08-24・旧スクリプト）

| | 既定温度 | 温度0 |
| --- | --- | --- |
| 成功率 | 6/6 = 100% | 6/6 = 100% |
| 平均秒 | ~~18.9秒~~ | ~~4.4秒~~ |

秒は初回ロード込みの平均なので読めない（1-2 で採り直した）。

道具は毎回 `['get_national_baseline', 'lookup_insurer']` を並列で呼んでいた。
メイン機の 7B は `lookup_insurer` だけで、基準値を **0/6** でしか引かなかった。
指示追従に明確な差がある。

### 1-2. 済んだこと（2026-08-24・改良版スクリプト）

**秒数の再計測。** 「18.9秒 → 4.4秒」は温度ではなく**初回ロード**だった。

| qwen3.5:9b | 既定温度 | 温度0 |
| --- | --- | --- |
| 成功率 | 6/6 | 6/6 |
| 指示追従 | 6/6 | 6/6 |
| 初回 | 19.2秒 | 6.8秒 |
| 初回を除く平均 | 3.2秒 | 3.9秒 |

温まった後は温度で差がない。1-1 の結論は取り下げ。

**交絡の切り分け。** GPU機で 7B（`ollama pull qwen2.5:7b`）も走らせた。

| qwen2.5:7b (GPU) | 既定温度 | 温度0 |
| --- | --- | --- |
| 成功率 | 5/6 | 6/6 |
| 指示追従 | 2/6 | **0/6** |
| 初回 | 36.1秒 | 3.3秒 |
| 初回を除く平均 | 0.5秒 | 0.4秒 |

メイン機の同じモデル（CPU）は 既定 4/6・温度0 6/6・指示追従 0/6・温まった後 7.6秒。

**GPU に載せても品質は動かなかった。**既定温度でのタグ崩れは残り
（漏れ方も同じ形: `brtc {"name": "lookup_insurer", ...} </tool_call>`）、
温度0での指示追従は 0/6 のまま。動いたのは秒数だけで 7.6秒 → 0.4秒。

つまり **成功率と指示追従はモデル世代の効果、速さはハードの効果**。
ついでに 9B は同じ GPU で 7B の8倍遅い（3.2秒 vs 0.4秒）。
道具を2つ並列に呼んで指示に従うぶん出力が長い＝速さと指示追従のトレードオフ。

### 持ち帰る数字

スクリプトが出す最後の3行をそのままコピーすれば足りる。

- 成功率（○/6）を温度ごとに
- **指示追従（○/6）** ← 今回いちばん差が出た軸
- 秒（中央値 / 初回 / 初回を除く平均）

---

## 2. エージェントを通しで走らせる

```
python -m kaigo_mcp.agent --column B-local-gpu --verbose "尼崎市は特養が足りてる？"
```

メイン機（列A・CPU）の結果は **2ステップ / 72.31秒 / 入力1,648・出力171トークン**。
答えは「18.1人で全国平均24.6人を下回る」で、これは正しい（検算済み）。

同じ質問で、ステップ数・秒数・答えの正しさを比べる。

```
python -m kaigo_mcp.agent --column B-local-gpu --max-steps 8 --verbose "青森県で特養が足りない市町村を3つ挙げて、全国と比べてどうか説明して"
```

### 2-1. 済んだこと（2026-08-24）

**落とし穴は当たった。ただし VRAM ではなくコンテキスト長で、しかも予想より手前。**

`ollama` の既定 `num_ctx` は **4096**（`ollama ps` の CONTEXT 列で見える）。
9B が 6.6GB を占める前に、この 4096 が先に天井になる。

| 「尼崎市は特養が足りてる？」 | 列A 7B (CPU) | 列B 9B (既定4k) | 列B 9B (8k) |
| --- | --- | --- | --- |
| ステップ | 2 | 2 | 3 |
| 秒 | 72.31 | 75〜89 | 21.6〜37.2 |
| 入力トークン | 1,648 | 1,985 | 3,176 |
| 出力トークン | 171 | **3,121** | 601 |
| 答え | 正しい | **空（0/4回）** | 正しい（3/3回） |

4k では毎回まったく同じ 3,121 トークンを吐いて**答えが空**になる。
中を見ると 2ステップ目が `stop_reason='length'`、
プロンプト約1,150 + 出力2,946 = **ちょうど4,096** で切れている。

引き金はモデル自身の**誤った引数**だった。尼崎市は兵庫県なのに
`lookup_insurer({'name': '尼崎市', 'pref': '大阪'})` を投げ、該当0が返る。
そこから2,946トークン考え込んで、閉じる前にコンテキストを使い切る。
**自己回復に要るトークンがコンテキストを食い潰す**のが本体で、
「賢さが落ちる前にコンテキストが落ちる」の中身はこれ。

8k に上げると解消する（`ollama create qwen3.5:9b-ctx8k -f` / `PARAMETER num_ctx 8192`）。
ただし **`ollama ps` が `12%/88% CPU/GPU` になる**。8GB には載りきらない。
ここで初めて VRAM の壁が見える。

難しい方の質問（`--max-steps 8`）は 4k でも完走した。

| 「青森県で…3つ挙げて」 | 列B 9B (既定4k) | 列B 9B (8k) |
| --- | --- | --- |
| ステップ | 3 | 3 |
| 秒 | 41.06 | 46.77 |
| 入力 / 出力 | 3,702 / 1,010 | 3,702 / 932 |
| 答え | 正しい | 正しい |

**質問の難しさではなく、回復の要否でコンテキストが決まる。**
`--max-steps` を上げても落ちなかったのはそのため。

### 2-2. 済んだこと — 空答えを「完走」と数えなくなった

直す前の `loop.py` は、道具呼び出しが無ければ `stopped_by = "end"` にしていた。
`stop_reason='length'` で切られて本文が空でも **"end"** で、
上の 4k の4回は記録上「2ステップで完走」と、列Aの成功と同じ形に見えていた。

`stop_reason == "length"` または本文が空なら `stopped_by = "truncated"` を立てる。
`stop_reason` も `RunRecord` と `.agent-usage.jsonl` に残す。

| 最後の往復 | stopped_by |
| --- | --- |
| `length`・本文あり（途中まで書けた） | `truncated` |
| `length`・本文なし | `truncated` |
| `stop`・本文が空白だけ | `truncated` |
| `stop`・本文あり | `end` |

CLI は理由と `num_ctx` の可能性を出し、**終了コード1**を返す（正常系は0）。
途中まで書けていた本文は捨てずに出す。

集計するときは `RunRecord.answered`（= `stopped_by == "end"`）を見ること。

> **`.agent-usage.jsonl` の古い行に注意。** 修正前に走らせた4回は
> `stopped_by: "end"` のまま残っている（`output` が 3,121 のもの）。
> 累計を取るときは捨てる。

---

## 3. 結果の持ち帰り方

`.agent-usage.jsonl` に実行記録が溜まるが、**これは .gitignore されている**
（手元の計測結果なので共有しない、という案1と同じ扱い）。

数字を持ち帰るときは、コンソール出力をコピーするか、明示的に送る:

```
git add -f .agent-usage.jsonl
```

とはいえ普通は、上の「持ち帰る数字」をメモして戻るだけで足りる。

---

## やらないこと

- **列C・列D は走らせない。** どちらも課金が発生する（`DEEPINFRA_API_KEY` /
  `ANTHROPIC_API_KEY`）。Anthropic の残高は公開中の本番アプリと共有なので、
  使い切ると案1のデプロイ済みアプリが止まる。列が揃ってから、まとめて1回だけ回す。
- `data/` の中身を作り直さない。案4（kaigo_gap_analysis）が書き出したものを
  そのまま使う前提。更新は `python scripts/sync_data.py` で、案4のあるマシンから。
